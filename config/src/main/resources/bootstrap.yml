#Server port
server:
  port: 8888

  #Git repo location.
spring:
  cloud:
    config:
      username: user
      passord: password
      server:
        git:
          uri: https://github.com/hnandago87/spring-cloud-config